/* =========================================================================
   Year index · site.js — configuration for the top-level overview page

   This page spans every edition, so it declares no single year and no
   chapters of its own. The shared engine reads the empty chapter list and
   skips both the chapter dropdown and the prev/next chapter nav.
   ========================================================================= */

window.SITE_YEAR = "";                                 /* spans all editions */
window.SITE_REPO = "tingwei161803/ai-index-report";
window.SITE_CHAPTERS = [];                             /* no chapters here */
